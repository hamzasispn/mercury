import type React from "react"
import type { Metadata } from "next"
import { Geist, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"
import { fontScript, fontMichigan, fontBlock, fontKansas, fontBureau } from "../fonts"

const _geist = Geist({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Jersey Customizer - Design Your Custom Kit",
  description: "Create custom jerseys with real-time preview, PayPal checkout, and admin dashboard",
  generator: "v0.app",
  icons: {
    icon: [
      {
        url: "/icon-light-32x32.png",
        media: "(prefers-color-scheme: light)",
      },
      {
        url: "/icon-dark-32x32.png",
        media: "(prefers-color-scheme: dark)",
      },
      {
        url: "/icon.svg",
        type: "image/svg+xml",
      },
    ],
    apple: "/apple-icon.png",
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans antialiased ${fontScript.variable} ${fontMichigan.variable} ${fontBlock.variable} ${fontKansas.variable} ${fontBureau.variable}`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
